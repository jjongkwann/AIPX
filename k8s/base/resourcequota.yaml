apiVersion: v1
kind: ResourceQuota
metadata:
  name: aipx-quota
  namespace: aipx
spec:
  hard:
    requests.cpu: "50"
    requests.memory: 100Gi
    limits.cpu: "100"
    limits.memory: 200Gi
    persistentvolumeclaims: "20"
    pods: "100"
    services: "50"
